"""
rl/
---
Reinforcement Learning package for the Legal QA pipeline.

Modules:
  state   — builds the 6-component state vector
  actions — macro-action enum and sub-action dispatch
  rewards — 8 reward signal functions
  agent   — PPO policy + value network
  env     — LegalQAEnv (gym-style environment)
"""
